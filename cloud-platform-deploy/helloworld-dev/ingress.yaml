apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: helloworld-rubyapp-ingress
  namespace: helloworld-dev
spec:
  tls:
  - hosts:
    - helloworld-dev.apps.live-1.cloud-platform.service.justice.gov.uk
  rules:
  - host: helloworld-dev.apps.live-1.cloud-platform.service.justice.gov.uk
    http:
      paths:
      - path: /
        backend:
          serviceName: rubyapp-service
          servicePort: 4567
