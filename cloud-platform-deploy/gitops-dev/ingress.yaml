apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: helloworld-rubyapp-ingress
  namespace: gitops-dev
spec:
  tls:
  - hosts:
    - gitops-dev.apps.gitops-test.cloud-platform.service.justice.gov.uk
  rules:
  - host: gitops-dev.apps.gitops-test.cloud-platform.service.justice.gov.uk
    http:
      paths:
      - path: /
        backend:
          serviceName: rubyapp-service
          servicePort: 4567
